#include <iostream>
using namespace std;
int calculate(int x);
int a;
int main() {
	cout << "Put in a number, Please." << endl;
	cin >> a;
	cout << calculate(a) << endl;
}
int calculate(int x) {
	if (x == 4) {
		return 4;
	}
	else {
	return calculate(x + 1) * 2;
	}
}
