#include <iostream>
#include <ctime>
using namespace std;
int main() {
	srand(time(NULL));
	while (true) {
		int num = rand() % 100;
		for (int i = 0; i < num; i++)
			cout << " ";
		cout << " * " << endl;

	}

 }
