#include <iostream>
#include <ctime>
#include <string>
using namespace std;
string input;
int main() {
	
	srand(time(NULL));
	while (input.compare("quit") != 0) {
		int num = rand() % 50 + 1;
		cout << num << endl;
		system("pause");
	}

}
